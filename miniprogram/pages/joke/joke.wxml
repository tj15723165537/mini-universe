<view class="content">
    <view class="main">
        <view class="card">
            <view class="text" wx:for="{{dataList}}" wx:key="{{index}}"  wx:if="{{item.gaoxiao}}">
                    {{item.gaoxiao}}
            </view>
        </view>
    </view>
</view>
